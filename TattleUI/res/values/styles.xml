<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <style name="popupTheme" parent="android:Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowBackground">@android:color/darker_gray</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowSoftInputMode">stateUnspecified|adjustPan</item>
    </style>

    <style name="screen_capture_rootlayout_style">
        <item name="android:weightSum">2</item>
    </style>

    <style name="screen_capture_components_style">
        <item name="android:weightSum">1</item>
        <item name="android:layout_weight">1</item>
    </style>
     <style name="screen_capture_email_radio_style">
        <item name="android:layout_weight">1</item>
    </style>
    <style name="screen_capture_record_audio_style">
        <item name="android:layout_weight">1</item>
    </style>

   
</resources>